{
  "name": "secret-writer",
  "version": "1.0.0",
  "description": "writes an AWS secret-manager secret to the file system",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@bbc/cps-mi6": "^1.0.0",
    "proxy-agent": "^3.0.3"
  },
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
